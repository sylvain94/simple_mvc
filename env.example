# 🔧 Configuration Environment Variables for Simple MVC Application

# =============================================================================
# 🌐 API Backend Configuration
# =============================================================================

# API Backend URL Configuration
# Options:
#   - "auto": Auto-detect VM's IP address and build URL automatically
#   - Full URL: Specify complete URL (e.g., https://192.168.1.141:8443)
# Default: auto
# Note: Both frontend and backend applications are assumed to be on the same VM
API_BACKEND_URL=auto

# Protocol for auto-detected URL (only used when API_BACKEND_URL=auto)
# Default: https
API_BACKEND_PROTOCOL=https

# Port for auto-detected URL (only used when API_BACKEND_URL=auto)
# Default: 8443
API_BACKEND_PORT=8443

# SSL verification for API calls (on/off)
# Default: off (useful for self-signed certificates)
API_SSL_VERIFY=off

# =============================================================================
# ⏱️ API Timeout Settings (Optional)
# =============================================================================

# Connection timeout for API calls
# Default: 30s
API_CONNECT_TIMEOUT=30s

# Send timeout for API calls
# Default: 30s
API_SEND_TIMEOUT=30s

# Read timeout for API calls
# Default: 30s
API_READ_TIMEOUT=30s

# =============================================================================
# 🐳 Docker Configuration
# =============================================================================

# Node.js environment
# Values: development, production
NODE_ENV=production

# =============================================================================
# 📝 Usage Instructions
# =============================================================================

# 1. Copy this file to .env:
#    cp env.example .env
#
# 2. Modify the values according to your environment
#
# 3. Use with Docker Compose:
#    docker compose --env-file .env up -d
#
# 4. Or export variables in your shell:
#    export API_BACKEND_URL=https://your-api-server.com:8443
#    docker compose up -d

# =============================================================================
# 🔍 Examples for Different Environments
# =============================================================================

# Auto-detection (Recommended - detects VM's IP automatically):
# API_BACKEND_URL=auto
# API_BACKEND_PROTOCOL=https
# API_BACKEND_PORT=8443

# Development Environment (Local):
# API_BACKEND_URL=http://localhost:3000
# API_SSL_VERIFY=on

# Development Environment (Auto-detect with HTTP):
# API_BACKEND_URL=auto
# API_BACKEND_PROTOCOL=http
# API_BACKEND_PORT=3000

# Staging Environment (Explicit URL):
# API_BACKEND_URL=https://staging-api.example.com:8443
# API_SSL_VERIFY=on

# Production Environment (Explicit URL):
# API_BACKEND_URL=https://api.production.com:443
# API_SSL_VERIFY=on
# API_CONNECT_TIMEOUT=60s
# API_SEND_TIMEOUT=60s
# API_READ_TIMEOUT=60s

# Production Environment (Auto-detect):
# API_BACKEND_URL=auto
# API_BACKEND_PROTOCOL=https
# API_BACKEND_PORT=443
# API_SSL_VERIFY=on
